import { Guid } from 'guid-typescript';

export class Driver {
    id: Guid;
    firstName: string;
    lastName: string;
    pesel: number;
    phoneNumber: number;
    carModel: string;
    registrationNumber: string;
    seatsAmount: number;
    salary: number;
    }