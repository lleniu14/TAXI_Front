import { Guid } from 'guid-typescript';

export class DriverOrder {
    firstName: string;
    lastName: string;
    distance: number;
    time: string;
    seatsAmount:number;
    address: string;
    }